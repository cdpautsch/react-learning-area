# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.
# These are technically superfluous, but maintained in case these projects were ever put into their own repos

# dependencies
/node_modules
/.pnp
.pnp.js
package-lock.json
/dist/*

# testing
/coverage

# production
/build

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# SSL
# Keys and certs should never be committed, even if used only for development
*.cert
*.key
